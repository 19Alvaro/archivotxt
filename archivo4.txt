 HEAD
para saltar a la rama original

git checkout main

realizamos los cambio o creamos nuevos archivos

touch archivo4.txt  en este caso vamos a crear un nuevo archivo

luego el poder de add + commit

git .
git commit -m "Nuevos comandos"

y el final maestro
git push origin main

clonar el repositorio 

git clone <url del repositorio>

cambiamos a la carpeta con cd archivotxt  es opcional .git

creamos una nueva rama con el comando

git checkout -b mi-nueva-rama

el -b le dice a Git crea la rama y cambiate a ella de una vez

hacemos nuestros cambios 

guardar y registar 

preparamos el archivo git add .

confirmamos el cambio git commit -m "Aqui esta un nuevo archivo"

simplemente soy todo "Linux"
 reposi
